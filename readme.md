# Landing Page Course from ITVDN

## Before start project you need:

> node -v
> npm -v
> npm install --global gulp-cli

## For start project you need:
> git clone [path-to-github-folder-with-project]
> npm install 